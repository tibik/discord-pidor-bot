{
  "name": "pidor-bot",
  "version": "2.0.0",
  "description": "A bot for pidor-of-the-day game",
  "main": "run.js",
  "scripts": {
    "start": "node pg.js",
    "init": "node install.js && node run.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --inspect run.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.6.4",
    "dotenv": "^6.2.0",
    "pg": "^8.4.1",
    "sqlite3": "^4.2.0"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
